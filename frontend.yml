- name: configure frontend
  hosts: frontend
  become: yes
  tasks:
  - name: install nginx 
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: satrt and enable nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes